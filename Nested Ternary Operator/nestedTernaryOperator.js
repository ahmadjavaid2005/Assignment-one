// Question No 3  :  Nested Ternary Operator
function checkNumber(num) {
     const result = num > 0 ? "positive" : num < 0 ? "negative" : "zero";
     console.log(result);
}

// Example usage:
const testNumber = -7;
checkNumber(testNumber);
